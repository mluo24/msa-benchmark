# Runs the benchmark of each algorithm to benchmark and outputs results

# import os
import subprocess
import time

# put some algorithm outputs


def run_command(cmd, out):
    subprocess.Popen(cmd, stdout=out)


def main():
    print("Multiple Sequence Alignment (MSA) Benchmark")

    print("Now running ...")

    print("Summary:")



if __name__ == "__main__":
    main()
